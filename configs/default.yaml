example_name: "000000000003.tif"
device: "cuda"
seed: 42

data:
  rgb_dir: "D:/path/to/rgb"
  gt_dir: "D:/path/to/gt"
  mask_dir: "D:/path/to/mask"
  da_dir: "D:/path/to/relative_depth"
  prior_dir: "D:/path/to/prior"

model:
  dino_repo: "C:/Users/your_user/dinov3"
  dino_ckpt: "D:/path/to/dinov3_vitl16_pretrain_sat493m-eadcf0ff.pth"
  in_dim: 1024
  lora_r: 8
  lora_alpha: 16.0

data_params:
  dsm_band: 6
  mask_band: 3
  da_band: 1
  prior_band: 3
  patch_size: 16
  stride: 4

optim:
  tto_steps: 100
  lr: 0.001

norm:
  mean: [0.430, 0.411, 0.296]
  std: [0.213, 0.156, 0.143]

output:
  root_dir: "outputs"
